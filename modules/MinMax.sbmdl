module STD_SUGARS;
requires: STD;

#define STD_SUGARS

#ephemeral </ "+object(
#ephemeral /> )+"

#define ,( tuple..(

#define procof(_P) Procedure.Delegate("_P")

#define await( operator..Await(

#define $len( global..len(

#define static_cast< cast..static<
#define dynamic_cast< cast..dynamic<
#define convert_cast< cast..convert<
