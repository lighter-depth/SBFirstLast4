module STD_SUGARS;
requires: STD;

#define STD_SUGARS

#ephemeral </ "+object(
#ephemeral /> )+"

#define ,( tuple..(

#define typeof(_T) Operators..TypeOf<_T>()
#define procof(_P) Procedure.Delegate("_P")

#define await( Operators..Await(
